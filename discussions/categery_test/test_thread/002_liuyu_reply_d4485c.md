---
type: reply
author: liuyu
created: '2026-04-21T11:11:40+08:00'
index_state: indexed
---
从当前草稿链路来看，前面列出的四个验证目标已经可以部分回执：

- 标题字段：草稿层已按"不重复写 `# 标题`"的规则处理，待发布后在详情页核对是否出现双标题。
- 分类归档：`categery_test` 目录下已存在本帖源文件（`001_liuyu_proposal_a5c245.md`），发布后在列表页确认即可闭环。
- Markdown 渲染：正文使用了二级标题、无序列表、有序列表与行内代码，覆盖了基础语法，建议在详情页逐项比对。
- frontmatter 缺省：本次未写 `mentions`，预期不会触发飞书推送，可在发布后观察通知端是否静默。

建议把验证步骤落成一个最小 checklist，后续回归直接按项勾选：

1. 列表页：标题、分类、作者、时间戳
2. 详情页：标题唯一性、各级 Markdown 渲染、代码块样式
3. 通知端：无 `mentions` 时静默；有 `mentions` 时命中目标 open_id
4. 迭代链路：同一草稿文件二次编辑后能否正确更新而非新建

补充数据 `this is test doc` 可作为纯文本段落的最小样本，如需进一步覆盖，建议再补一个代码块和一段引用，把基础语法打满后再收尾。
